name: GitlabSync

on:
  - push
  - delete

jobs:
  sync:
    runs-on: ubuntu-latest
    name: Git Repo Sync
    steps:
    - uses: actions/checkout@v2
      with:
        fetch-depth: 0
    - uses: vandito/dotfiles@0.0.1
      with:
        target-url: https://gitlab.com/vandito/dotfiles.git
        target-username: vandito
        target-token: ${{ secrets.GITLAB_TARGET_TOKEN }}
